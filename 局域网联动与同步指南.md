# Mac 监控服务 + Windows 桌宠使用指南

这套联动包含两条互不干扰的链路：

- 代码同步：两个项目通过 GitHub 拉取和推送，在 Mac 与 `starl` 之间转移开发进度。
- 运行时联动：Mac 监控服务通过局域网把新事件实时推送给 Windows 桌宠。

监控数据、Cookie、LLM Key 和配对令牌都只保存在各自设备，不会提交到 GitHub。

## 一、首次连接

### 1. 在 Mac 启动监控

打开 Mac 上的 `ua-monitor` 目录，双击：

```text
启动监控服务.command
```

脚本会自动完成以下工作：

1. 创建 Python 虚拟环境（仅首次需要）。
2. 安装后端依赖（仅首次需要）。
3. 生成只读插件的随机配对令牌。
4. 在局域网地址和 `8012` 端口启动服务。
5. 显示桌宠需要填写的“服务地址”和“配对令牌”。

当前 Mac 的地址通常是：

```text
http://192.168.50.13:8012
```

以启动窗口实际显示的地址为准。macOS 第一次询问是否允许 Python 接收网络连接时，选择“允许”。

### 2. 在 starl 配置桌宠

启动桌宠，右键桌宠，依次打开：

```text
设置 -> 监控
```

填写：

- 服务地址：Mac 启动窗口显示的地址。
- 配对令牌：Mac 启动窗口显示的令牌。
- 接收监控服务的新事件：开启。
- Windows 系统通知：按需开启。

先点“测试连接”，看到连接成功后点“保存并连接”。以后启动桌宠会自动连接，不需要重复配置。

### 3. 日常使用

Mac：

```text
双击 启动监控服务.command
```

Windows：

```text
正常启动桌宠
```

监控命中新事件后，桌宠会立即切换提醒表情、显示事件标题和摘要；如果开启了系统通知，还会显示 Windows 通知。网络短暂中断时会按 1、2、4、8、16、30 秒间隔自动重连。

停止 Mac 监控时双击：

```text
停止监控服务.command
```

## 二、一键同步两个项目

两个仓库根目录都提供同名入口。

Mac：

```text
双击 同步全部项目.command
```

Windows：

```text
双击 同步全部项目.cmd
```

默认同步会依次执行：

1. 自动找到桌宠和监控仓库；缺少的仓库自动克隆。
2. 把未提交改动做成一个带设备名和时间的 checkpoint 提交。
3. 从 GitHub 拉取对方设备的新提交并执行 rebase。
4. 将本机提交推送到 GitHub。

Windows 第一次推送时，Git Credential Manager 可能要求登录 GitHub，这是正常的一次性步骤。

只想下载、不想提交或上传时，在终端使用：

Mac：

```bash
./scripts/sync-workspace.sh --pull-only
```

Windows PowerShell：

```powershell
.\scripts\sync-workspace.ps1 -PullOnly
```

### 推荐开发顺序

每次开始开发前先运行一次“同步全部项目”，开发结束后再运行一次。这样 Mac 的 Codex 插件和 `starl` 上的 Codex App 都能从同一份 Git 历史继续。

尽量不要让两台设备同时修改同一个文件的同一段代码。若确实发生冲突，脚本会停在冲突现场，不会覆盖任一边：

```bash
git status
# 编辑并解决冲突
git add <已解决的文件>
git rebase --continue
```

解决后重新运行同步脚本即可。

## 三、在任意设备独立运行

两个项目都会克隆到同一个工作区中，但仍是两个独立 Git 仓库：

```text
工作区/
├── ua-pet-tauri/
└── ua-monitor/
```

如果 Mac 不在线，也可以在 `starl` 的 `ua-monitor` 目录双击：

```text
启动监控服务.cmd
```

然后把桌宠的服务地址改为：

```text
http://127.0.0.1:8012
```

并填写 Windows 启动窗口显示的令牌。此时监控和桌宠都在 `starl` 本机运行。切回 Mac 服务时，只需把地址和令牌改回 Mac 显示的值。

## 四、哪些内容会同步

会同步：

- TypeScript、Rust、Python、Android 等源代码。
- 项目文档、测试、构建脚本。
- 项目声明中允许提交的图片和皮肤资源。

不会同步：

- `node_modules`、Python `.venv`、Rust `target`、前端 `dist`。
- `monitor.db`、日志和进程 ID。
- `.env`、`.env.lan`、B 站 Cookie、LLM API Key、配对令牌。
- 桌宠本机设置和交互历史。

这些内容不进入 Git，是为了防止密钥泄露、数据库写入冲突和跨系统依赖损坏。

## 五、连接故障

### 测试显示“无法连接”或超时

1. 确认 Mac 的启动窗口显示“监控服务已启动”。
2. 确认两台设备连接同一个路由器网络。
3. 在 Mac 重新双击启动脚本，检查地址是否变化。
4. 允许 macOS 防火墙中的 Python 接收连接。
5. 在 Windows 浏览器打开 `http://Mac地址:8012/health`，正常会看到 `{"status":"ok"}`。

### 测试显示“配对令牌不正确”

重新复制 `ua-monitor/backend/.env.lan` 中 `BILI_MONITOR_PLUGIN_TOKEN=` 后面的内容，或直接复制启动窗口显示的令牌。

### 测试显示“Mac 端还没有启用桌宠插件接口”

说明监控仓库代码较旧或 `.env.lan` 没有生成。先运行“同步全部项目”，再停止并重新启动监控服务。

### 服务启动失败

Mac 日志：

```text
ua-monitor/backend/lan-monitor.log
```

Windows 日志：

```text
ua-monitor/backend/lan-monitor.out.log
ua-monitor/backend/lan-monitor.err.log
```

### 8012 端口被占用

Mac 终端：

```bash
DGATES_MONITOR_PORT=8013 ./scripts/start-lan-monitor.command
```

Windows PowerShell：

```powershell
$env:DGATES_MONITOR_PORT="8013"
.\scripts\start-lan-monitor.ps1
```

随后把桌宠地址末尾端口改为 `8013`。

## 六、安全边界

- 远程设备只能访问 `/api/plugin/v1` 只读接口。
- 监控对象、扫描参数等管理接口只允许监控主机本机访问。
- 插件接口必须携带随机令牌。
- 配对令牌不会写入 Git；不要把 `.env.lan` 发到公开位置。

要更换令牌，先停止监控，删除 `ua-monitor/backend/.env.lan`，再重新启动。桌宠中同步更新新令牌即可。
