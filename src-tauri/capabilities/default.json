{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "mask-editor"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
