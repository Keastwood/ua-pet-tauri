{
  "name": "desktop-pet-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "skin:generate": "node scripts/generate-skin.mjs",
    "skin:generate:heart": "node scripts/generate-skin.mjs --image ../爱心眼ua.png --id heart-eye-ua --name \"爱心眼 UA\" --layout fullBody",
    "skin:refresh": "node scripts/generate-skin.mjs --refresh",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "sharp": "^0.34.5",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
